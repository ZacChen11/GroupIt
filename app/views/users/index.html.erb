<h1>All Users</h1>

<ul id="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.user_name, user_path(user), class: 'user_title' %>
    </li>
  <% end %>
</ul>

<%= link_to "Create a New User", new_user_path, class: "new_project" %>